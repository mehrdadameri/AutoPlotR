% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/live.R
\name{ap_live}
\alias{ap_live}
\title{Launch the AutoPlotR live chat interface}
\usage{
ap_live(
  data = NULL,
  root = getwd(),
  config = NULL,
  provider = NULL,
  model = NULL,
  api_key = NULL,
  launch = interactive(),
  launch_browser = interactive()
)
}
\arguments{
\item{data}{Optional data frame. If NULL, load data via chat (list_files ->
load_data) or let the agent detect data in your RStudio environment.}

\item{root}{Working directory for file scanning and plot output.
Defaults to \code{getwd()}.}

\item{config}{Optional AutoPlotR config path, text, or list.}

\item{provider}{Optional LLM provider override.}

\item{model}{Optional LLM model override.}

\item{api_key}{Optional API key for this session.}

\item{launch}{Whether to launch the chat UI.}

\item{launch_browser}{Use background process + browser (non-blocking).
Set to FALSE for terminal mode (blocks R thread until exit).}
}
\value{
An \code{ap_live_runtime} list, invisibly. For background mode,
returns a list with background job details.
}
\description{
Opens an interactive chat session for data exploration and visualization.
The agent can scan directories, load data files, detect datasets in your
RStudio environment, profile columns, and generate ggplot2 plots -- all
through conversation.
}
\details{
When \code{launch_browser = TRUE}, the chat runs in a \strong{background R process}
so your R console stays free. The chat opens in your web browser and you
can continue running code in RStudio while chatting. After the chat ends,
call \code{\link[=ap_fetch_data]{ap_fetch_data()}} to load modified data back into your environment.

When \code{launch_browser = FALSE}, the chat runs in the terminal and blocks
the R thread until you exit (Esc / Ctrl+C).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
ap_live(mtcars)
ap_live(root = "~/project")  # scan directory, load files, plot
\dontshow{\}) # examplesIf}
}
